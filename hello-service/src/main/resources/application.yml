server:
  port: 9092
spring:
  application:
    name: HELLO-SERVICE

eureka:
  client:
    registry-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instanace:
    hostname: localhost

server:
  port: 9093

spring:
  application:
    name: LOG-SERVICE

eureka:
  client:
    registry-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instanace:
    hostname: localhost
